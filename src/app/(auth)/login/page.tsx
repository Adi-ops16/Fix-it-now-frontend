import LoginForm from "../_components/LoginForm";

export const metadata = {
    title: "Login - Fix-it Now",
    description: "Sign in to your Fix-it Now account",
};

export default function LoginPage() {
    return <LoginForm />;
}

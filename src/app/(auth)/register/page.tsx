import RegisterForm from "../_components/RegisterForm";

export const metadata = {
    title: "Register - Fix-it Now",
    description: "Create your Fix-it Now account",
};

export default function RegisterPage() {
    return <RegisterForm />;
}
